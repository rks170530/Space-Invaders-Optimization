Instructions to Run:
	python atary.py -tsl 500000 -trl 20

You can of course change the parameters, but these are the simple ones we used.


You can use:
	python atary.py -h

For help on what the parameters mean but they are explained in the report.